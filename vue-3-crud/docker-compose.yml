version: '3'
services:
  backend:
    image: your-spring-boot-app
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080

  frontend:
    image: your-vue-app
    ports:
      - "8081:8081"
    environment:
      - VUE_APP_PORT=8081
